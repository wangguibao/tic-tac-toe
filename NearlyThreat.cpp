#include "NearlyThreat.h"

NearlyThreat::NearlyThreat(const Chess &chess_, const Chess &missing1_,
                           const Chess &missing2_)
: chess(chess_), missing1(missing1_), missing2(missing2_)
{
}
